__*Грамматика записывается в файл*__ ```grammar.txt``` в следующем виде:
```
S -> Ab
A -> aA | b
...
```
__*Слово записывается в файл*__ ```word.txt```

__Запуск через консоль:__
```bash
./run.sh
```
